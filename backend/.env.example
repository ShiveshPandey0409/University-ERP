# Copy to backend/.env
DB_HOST=127.0.0.1
DB_PORT=1433
DB_NAME=PSNSUniversityOnline
DB_USER=sa
DB_PASSWORD=Ptsnsu_Dev#2026
DB_DRIVER=ODBC Driver 18 for SQL Server

ENVIRONMENT=development
CORS_ORIGINS=["http://localhost:5173"]
# Set to the built React directory when FastAPI should serve the SPA.
# FRONTEND_DIST=/app/frontend/dist

# generate: python -c "import secrets;print(secrets.token_urlsafe(48))"
JWT_SECRET=please-generate-a-long-random-secret

# Payment gateway secrets (set real values only in .env / Render secrets)
SABPAISA_CLIENT_CODE=
SABPAISA_TRANS_USER=
SABPAISA_TRANS_PASS=
SABPAISA_AUTH_KEY=
SABPAISA_AUTH_IV=
SABPAISA_CALLBACK_URL=http://localhost:8000/payments/callback
